services:
  db:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: yunto
      POSTGRES_PASSWORD: yunto
      POSTGRES_DB: yunto
    ports:
      - "5432:5432"
    volumes:
      - yunto_pg:/var/lib/postgresql/data
volumes:
  yunto_pg:
